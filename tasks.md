<!-- read in full — kept under 150 lines. Completed work is in tasks/done-archive.md. -->
# Task Board

Actionable work items (agents and humans). Derived from `TODO.md` (human roadmap).
See `AGENTS.md` → Task Management for conventions. This repo uses root `tasks.md`
as the single task board — do NOT create `.agents/tasks.md`.

**Done items do not stay on the board.** When an item closes, move it — with its date and closing
note, verbatim — to `tasks/done-archive.md`, the only other task file and the one that holds
nothing actionable. Anything live is in *this* file. Same convention as the partle and
kart-medulla repos. One exception: a **done step of a task that is still open** stays put — a
finished step loses its context once the decision it settled is filed away, so a cluster moves to
the archive whole, only once its last step closes.

## Ready

- [ ] **`ESP_HEALTH_STATUS` loses its last field in transit — the steering trip age never reaches the dashboard.** Found 2026-08-10 while answering "where is the reject counter". The firmware assembles seven int32 (`main/main.c`, `int32_t payload[7]` = `[flags, agc, heap_kb, i2c_errors, steer_frames, steer_rejects, steer_trip_age_s]`) and calls `KM_COMS_SendMsg(ESP_HEALTH_STATUS, payload, 7)`. `kb_coms_micro` splits `flags` off and forwards the rest, so `/esp32/health/data` should carry six. It carries **five**: observed `[0, 338, 0, 977300, 0]` and again `[0, 338, 0, 2354996, 0]`, i.e. `steer_trip_age_s` is absent and `protocol.decode_health_data`'s length guard leaves `health_steer_trip_age_s` unset.
  **This defeats the fix deployed the same day.** `20db00f` stopped `decode_health` dropping appended fields precisely so a stale latch could be told from a live one — the ambiguity that cost hours on 2026-08-08. The decoder is now correct and the field still never arrives, so the dashboard cannot show trip age at all.
  **Already ruled out:** the C++ handler does not truncate (`health_data_msg.payload.assign(frame_esp.payload.begin() + 1, frame_esp.payload.end())`, no cap); `SerialDriver::MAX_PAYLOAD` is 255; and neither side is a stale build — `kb_dashboard` and `kb_coms_micro` were rebuilt and the ESP32 flashed from `be82616` on 2026-08-10, and `steer_frames` climbs normally so the link is healthy. The loss is therefore between `KM_COMS_SendMsg` and the C++ handler: suspect the framing layer's LEN handling or the ESP32-side serialiser writing one element short. Next step is to dump a raw `ESP_HEALTH_STATUS` frame off `/dev/ttyACM0` and count the bytes — 7 int32 plus header should be 28 payload bytes.

- [ ] **The BATT dial's SOC is the BMS's raw coulomb count and it has drifted — 93% shown at 46.4 V** (added 2026-08-10). `kb_bms/bms_node.py:82` reads `soc = d[19]` from the JBD basic-info frame and passes it through untouched (`msg.percentage = soc/100`, line 396) to `dashboard_node.py:391` and the dial. A JBD SOC only re-zeros on a full-charge or low-voltage-cutoff event; between those it is an open-loop integral. Observed 2026-08-10: dial read 93% while the pack sat at 46.4 V, which on this 13S4P Molicel P42A pack is 3.57 V/cell — roughly 25-30% resting. 93% would need about 52.7 V (the first ever BMS reading, logged in `history.md`, was 52.84 V at SOC 99%).

  **Plan (not started).** Fuse rather than replace. Voltage alone is a weak SOC sensor here: the P42A curve is flat from ~3.6 to ~3.9 V/cell, and pack resistance is roughly 65 mOhm (~20 mOhm/cell, x13 series, /4 parallel), so a 48 A draw sags the pack about 3 V — the whole flat region. Nor should kart-brain run its own coulomb counter: the BLE poll loop in `bms_node.py` completes about every 2.2 s (~0.45 Hz), far too slow to integrate spiky throttle current without aliasing. The BMS integrates at high rate internally; its fault is offset, not rate. So: predict with the BMS's own change in `remain_ah` between polls, correct with an OCV-derived SOC (OCV = terminal V minus I*R_pack, JBD sign convention positive = charge), and schedule the measurement noise on the local OCV slope and on |current| — that makes the filter ignore voltage in the flat plateau and under load, and trust it at rest and near the ends, without a hand-tuned mode switch.

  **Blocked on data, not code:** needs a measured OCV-vs-SOC table for this pack and a pack resistance figure. One instrumented full discharge logging V, I and `remain_ah` from `/battery/state` yields both (resistance from dV/dI across consecutive samples where current stepped).

  **Do first, independently of the filter:** seed the SOC estimate from OCV at boot, when the kart is nearly always at rest. That alone catches this failure. Also recalibrate the BMS itself once by running to the low-voltage cutoff and then charging to termination.

  **Display rule:** show fused SOC as the dial's number and keep the raw BMS SOC visible in the Battery tab. Disagreement between the two is the drift, and it is the thing worth seeing.

  **Filter built 2026-08-10 in `6c679b5`, still uncalibrated.** `src/kb_bms/kb_bms/soc_model.py` holds the OCV curve, the sag compensation and the scalar Kalman filter; `bms_node` runs it each poll and publishes the result on `/battery/soc_fused`; the dashboard's Battery tab prints `V says NN%` under the SOC ring when the two figures differ by more than 10 points. 14 tests in `src/kb_bms/test/test_soc_model.py` cover the behaviour (seeds from voltage at rest, resists a sagging pack under load, follows the coulomb count across the plateau) and pass without ROS, so they run on a laptop. `msg.percentage` still carries the raw BMS number and the `soc_fusion` parameter defaults false.

  **What is left.** (1) Run `ros2 run kb_bms log_battery --ros-args -p path:=/tmp/battery-run.csv` across one full discharge and recharge. (2) Replace `OCV_TABLE` and `CELL_RESISTANCE_OHM` in `soc_model.py` with values fitted from that trace — they are currently generic NMC 21700 figures, not measurements of this pack. (3) Set `soc_fusion` true so the dial's number becomes the fused estimate. (4) Nothing here is deployed to the Orin yet: needs `git pull`, `colcon build --packages-select kb_bms kb_dashboard`, restart.

- [ ] **Watch the journal for `no internet through <dev>` — the report-only tether probe** (added 2026-08-10). `wifi-watchdog.sh` probes each USB tether once a minute with `curl --interface` and logs whether it actually reaches the internet, taking no action either way. It exists for the one failure carrier detection cannot see: a phone that keeps its link and DHCP lease while carrying no traffic (out of coverage, data allowance spent, operator blocking tethering). Hotspot-off is *not* that case — iOS drops the USB carrier and the kernel reroutes on its own, measured the same day. **If that log line ever appears, it is the first real evidence this failure happens on this kart**, and a threshold for demoting the dead tether can be set from it. Until then there is deliberately nothing acting on it, for the same reason the pump-stall detector in kart-medulla is report-only. If it never appears in a few months of running, delete the probe.
- [ ] **Deploy the STEER SIGNAL LOST dashboard wording to the Orin** (dev `9539561` + `a0…`; pushed 2026-08-08 but the Orin lost internet before pulling): `cd ~/kart-brain && git pull && sudo systemctl restart kart-brain`; if the text doesn't change, `colcon build --packages-select kb_dashboard` first. Also covers dev `2607de2` (pedals dial swapped to brake-left/accel-right, matching real pedal placement) , `df21245` (plain-language status readouts) and `6aab151` (Angle/PWM steering toggle made reachable; the gamepad readout it also added was removed again in `688a437`), pending on the Orin for the same reason.

  **Deployed 2026-08-10.** The wording itself turned out to be on the Orin already; what was missing
  was `20db00f`, which stops `decode_health` dropping the appended fields and decodes the steering
  trip age — the half that makes a stale latch distinguishable from a live one, so the entry was not
  as stale as it looked. Pulled, `colcon build --packages-select kb_dashboard kb_coms_micro`,
  restarted. Evidence: `/esp32/health/data` now publishes 5 fields (`[0, 338, 0, 977300, 0]`) where
  it published 3 before the rebuild, and the dashboard answers HTTP 200. `688a437` (gamepad support
  dropped, Angle/PWM toggle kept) shipped in the same pull. Awaiting Rubén's confirmation on screen.

- [x 2026-08-10] **Touching the remote-driving pad jumps straight to that value — a stray tap near the top edge is instant full throttle** — Fixed in `c82194e`, by a fourth option Rubén proposed that beats the three below: **the pad only accepts a grab that starts inside the centre ring**, and a touch outside it is ignored rather than reinterpreted. It needs no anchor state, leaves the absolute feel untouched once held, and matches a physical stick — you take hold of it where it rests. `JS_GRAB_RADIUS = 0.26` in `src/kb_dashboard/kb_dashboard/index.html` is the radius of `.js-detent` (26% of pad width), so the ring already drawn as neutral IS the target; change the two together. A refused grab flashes the ring amber, because silently ignoring a touch looks identical to a frozen dashboard. Verified in a browser: top-edge and right-edge taps now send nothing (both were full-scale commands), a centre grab engages, and a centre-to-top drag still reaches throttle 1.0. **Two things this deliberately does not do:** it does not slow a fast deliberate drag — that is a driving control working as intended — and grabbing at the far edge of the ring still commands up to 0.26 immediately, since inside the ring is 'holding the stick', not 'a stray touch'. Pending on the Orin with the other dashboard commits. Not yet tried with a thumb on a moving kart, which is the only test that can say whether the ring is big enough with gloves on.

- [x 2026-08-10] **A connected gamepad sends manual_control at 10 Hz even when nobody touches it, which fights the touch pad** — Moot: gamepad support was removed the same day it was filed (`688a437`), on Rubén's call that the dashboard makes it obsolete. The 10 Hz `navigator.getGamepads()` poll, its readout and the ROS-side `joy_node` → `joy_to_cmd_vel` → teleop launch are all gone, so nothing but the touch pad writes `manual_control` any more. Kept as a record because the shape of the bug outlives the gamepad: **two input sources writing the same command with no arbitration means the last one to arrive wins**, and an idle source that keeps sending zeros beats an active one. If a second input is ever added, it takes the controller token the way the touch pad does.


- [ ] **The Status panel is a full grid column showing three pills in remote_control** — Raised 2026-07-30. On the race skin's Mission page, `updateMissionUI` hides the Controls row (`.ctrl-row`, `.ctrl-grouplbl`) for every non-autonomous mission, so in `remote_control` the Status panel renders only three pills — state, mission, heartbeat — while still taking a full column of the `.rc-mission` grid, next to a joystick pane that is cramped. **This is a design decision that was never made, not lost work:** it was investigated as a suspected merge revert on 2026-07-30 and the Status panel provably has never been removed in any commit on any branch (full write-up in `history.md`, same date). Options: hide the Status pane in remote like `rcAlgoPane` already is and let the grid redistribute; or fold the three pills into the topbar so the state is visible on every page, not just Mission. The second is probably better — heartbeat and AS state are worth seeing while driving, and Mission is not the page you are looking at then. Whoever picks one should check the grid does not leave an empty column, which is the thing `164cdfe` was careful about. Update 2026-07-31: the joystick pane itself is no longer cramped — the pad now grows to fill it instead of sitting at a fixed 220x170 — but the Status panel still holds a full grid track for three pills, and the choice above is still open.

- [ ] **Accel pedal bar: real telemetry wired end to end on `feature/pedal-telemetry` — remains to flash, deploy, and calibrate on the kart** — Found 2026-08-08 (the accel bar read `/esp32/throttle`, which no firmware frame ever published, so it sat at 0 while throttle was commanded). Fixed the same day, option (b): kart-medulla branch `feature/pedal-telemetry` reads the pedal ADCs (GPIO 4 accel, GPIO 5 brake, halved 0-5 V through the board's 10k/10k dividers) and sends a new `ESP_PEDALS` frame (0x0E, ~20 Hz, `[acc_mv, brake_mv, acc_effort, brake_effort]`); kart-brain branch `feature/pedal-telemetry` forwards it as `/esp32/pedals` (kb_coms_micro), decodes it (`protocol.decode_pedals` → `esp32_throttle`, `esp32_brake_pedal`, raw mV keys), and the accel bar now shows real pedal (green) plus an amber `orin_cmd_throttle` target tick, same convention as brake. Remaining: merge both branches, `colcon build` on Orin + flash the ESP32-S3, then measure the real pedal endpoints on the kart and replace the provisional `PEDAL_MIN_MV`/`PEDAL_MAX_MV` (0/2500 mV, pure divider math) in kart-medulla `main/main.c` — until then the effort fields (and the bar) are approximate; the mV fields are exact.

- [ ] **Decide what the pedals dial's BRAKE bar should measure: the driver's brake pedal or the brake-line pressure** — Two entries in this file now disagree, and both are about the same red "real" fill in `rcDrawPedals` (`src/kb_dashboard/kb_dashboard/index.html`). On 2026-08-08 the bar was changed to read `esp32_brake_pedal`, the driver's brake-pedal ADC (ESP32-S3 GPIO 5, connector CN6.1, arriving in the `ESP_PEDALS` frame), because it previously read `esp32_braking` — the brake *actuator's* commanded effort — which meant the pedal reading never appeared on screen at all even though the ESP32 was publishing it correctly (live frame that day: `[419, 381, 42, 38]`, so 381 mV of brake pedal was being thrown away). The "Wire the pneumatic pressure sensors" entry below instead proposes that the piston/brake-line pressure sensor should drive this bar, so that one sensor feeds both the PISTON dial and the brake needle. These are three different physical quantities (pedal position, commanded actuator effort, resulting line pressure) and the dial can only show one as "real". Pedal position is the choice that matches the panel's ACCEL/BRAKE pedal framing and keeps both bars symmetric; line pressure is the choice that shows whether braking actually happened. Pick one, and if it is line pressure, say where the driver's brake pedal gets displayed instead — it must not go back to being invisible.

- [ ] **Both pedal bars read ~15% with no pedal pressed** — Confirmed on the kart 2026-08-08 from a live `/esp32/pedals` frame: `[419, 381, 42, 38]`, i.e. the accelerator rests at 419 mV and the brake at 381 mV at the ESP32-S3 pin, which the provisional calibration turns into efforts of 42 and 38 out of 255. This is the same uncalibrated span already flagged in the accel-pedal entry above (`PEDAL_MIN_MV 0` / `PEDAL_MAX_MV 2500` in kart-medulla `main/main.c`, derived from divider math and never measured), but it is worth its own line because it is now visible to anyone looking at the dashboard: two bars showing a sixth of travel while nobody is touching the pedals reads as a fault. Measure both pedals released and floored, put the real numbers in those two constants, and reflash.

- [ ] **`KM_PID_GetTunings` cannot read gains back, so the PID echo mirrors them by hand** — Found 2026-07-30 while adding live PID tuning. In kart-medulla, `components/km_pid/km_pid.c:114` defines `KM_PID_GetTunings(PID_Controller *controller, float kp, float ki, float kd)` with a body identical to `KM_PID_SetTunings` — it overwrites the gains, and taking floats by value means it could not return them even if it tried. Consequence on this side: `main.c` keeps its own `g_pid_kp/ki/kd/pwm_limit` file-scope copies purely to build the `ESP_STEER_PID` frame, so the gains exist in two places and can drift if a future code path sets tunings without updating the mirror. Filed in kart-medulla's `tasks.md` too, since the fix (pointer out-params, then delete the mirror) is firmware-side. Nothing to do in kart-brain unless the echo starts disagreeing with the dashboard.

- [ ] **Retire the AS5600 for good — it will never be used again** — Confirmed on the kart 2026-07-30. The steering sensor is an **MT6701 read as a ~994.4 Hz PWM angle frame on GPIO 1 (CN5.2)**, captured via MCPWM; driver is kart-medulla `components/km_sdir/km_sdir_pwm.c/.h`. Calibration lives in `km_sdir_pwm.h`: centre 1242 raw, full left lock 2328, 4096 steps/rev, and because the count rises to the left, positive = left needs no sign negation (ROS REP 103). Verified live: a steering frame read raw 1002 / -0.368 rad / valid flag 1, and 1002-1242 = -240 counts = -0.368 rad, so the maths is self-consistent. Health flags read 12, i.e. bit 3 `steer_ok` set.
    - **No firmware flash is required for anything to work** — the S3 build already skips the AS5600 (`#if !defined(CONFIG_IDF_TARGET_ESP32S3)` in `health_task`), and the dashboard-side misreporting was fixed in kart-brain `cc59be4`, which needs only a `git pull` on the Orin. This task is dead-code cleanup, not a fix.
    - **Cleanup, when someone wants it (this part does need a flash):** in kart-medulla, delete the `km_sdir.c`/`km_sdir.h` AS5600 I2C driver and its call sites, drop `HEALTH_FLAG_MAGNET_OK` (bit 0) and `HEALTH_FLAG_I2C_OK` (bit 1) plus the `agc` and `i2c_errors` fields from the health frame, and renumber or reserve those bits deliberately. In kart-brain, `decode_health` / `decode_health_flags` / `decode_health_data` in `src/kb_dashboard/kb_dashboard/protocol.py` must stay backwards-compatible with firmware that still sends the old shape, and the dashboard's now-N/A AS5600 card can then be removed outright rather than labelled.
    - **Do not simply renumber the flag bits without changing both repos together** — the health frame is a positional int32 payload, so a bit shift on one side silently mislabels every flag on the other. Whoever does this flashes the ESP32 and pulls kart-brain in the same session, then re-reads the flags to confirm.

- [ ] **`esp32_accel_lat` / `esp32_accel_lon` are named for the wrong source — they come from the ZED IMU** — Noticed 2026-07-30. `_on_zed_imu` in `src/kb_dashboard/kb_dashboard/dashboard_node.py:276-282` is the only thing that writes these keys, from `/zed/zed_node/imu/data`. No ESP32 frame populates them, so the `esp32_` prefix sends anyone tracing the G-G dial to the wrong subsystem — it cost one such trace already. Rename to `imu_accel_lat` / `imu_accel_lon` across `dashboard_node.py`, `protocol.py`'s `DashboardState` defaults, `index.html`'s consumers, and the demo block, in one commit so nothing reads a key that no longer exists. **No behaviour change is wanted:** the deliberate `-msg.linear_acceleration.y` flip (REP 103 y=left → screen positive=right) is correct as it stands and must survive the rename. Low priority, purely a naming fix.


- [ ] **`decode_pneumatic`'s docstring says compressor state 4 means "latched off"; the firmware keeps pumping** — Found 2026-07-30 reading a live frame. `src/kb_dashboard/kb_dashboard/protocol.py:291-293` documents state 4 as "pumping latched off after a full burst produced no pressure rise". In kart-medulla `main/main.c`, `pump_stall_observed` (declared line 66) is written in exactly one place (line 503) and read in exactly one place (line 527, the state code). It never gates `compressor_demand` or `comp_duty`. Nothing is latched off: after the forced 15 s cooldown the burst restarts, so a stalled system cycles 15 s on / 15 s off indefinitely while permanently reporting state 4. The firmware behaviour is intended — commit `b5f54f4` deliberately made the stall detector report-only after it had been wired to fire the EBS — so the fix is the kart-brain wording, not the firmware. Reword to say the diagnostic is latched while pumping continues. Worth checking the dashboard's own state-4 label text (`index.html` around line 1985) for the same claim.

- [ ] **Decide where the compressor should actually pump to, from scratch** — There is no calibration question left and there never was a real conflict. The sensor chain is fully documented: Festo SDE5 at 1 V/bar (datasheet), board divider 3:1 (R11/R12/R13 all 10K, nets `PRESSURE_n__0_10V` -> `PRESSURE_n__0_3V3`), and the firmware now converts counts to millivolts through the ESP32's eFuse calibration. `bar = 3 * V_pin`, no fitted constants.
    - **The "7.5 bar at ADC 2679" figure is void.** It came from one line in a 2026-07-18 commit message and cannot be used: there is no mechanical gauge on this kart, the wiring and firmware have both changed since, a regulator may sit between that measurement point and PRESSURE_1, and the two numbers may not have been taken at the same moment. Full write-up in kart-medulla `.agents/error-log.md` 2026-07-27. Do not resurrect it, and do not derive hardware values from mismatches involving it — that produced two confident wrong claims already.
    - **What is actually open:** `ADC_PRESSURE_LOW/HIGH` in kart-medulla `main.c` are still the raw counts 2500/2858 that were scaled from that void figure. By the sensor chain they are about 6.0 and 6.9 bar, not the 7 and 8 their old comment claimed. Nobody has decided what the right targets are. Pick them from the pneumatics requirement (`~/dv/kart/pneumatics/README.md`: ~6 bar minimum for ~3 EBS activations, reservoir rated 10 bar), convert to millivolts, and set the thresholds in mV so they mean a pressure instead of a count.
    - **Do this together with the ground-IR-drop bias** (kart-medulla `tasks.md`): the reading sags while the compressor runs, so a threshold evaluated against a running reading stops the pump later than intended. Setting new targets without accounting for that just moves the error.

- [ ] **(at the kart) Verify the shutdown-circuit drive on the bench, then wire the Q3 gate** — As of 2026-07-26 the ESP32 firmware drives `SDC_NOT_EMERGENCY` (GPIO 18) both directions for the first time: `control_task` closes the chain only while the Orin reports `AS_READY`/`AS_DRIVING` with comms fresh, no steering-fault latch, and the compressor enabled. **The gate is not connected to anything downstream**, so nothing physically arms or brakes. Work: (1) with the ESP32 flashed and an autonomous mission selected, confirm the dashboard's EBS page shows "Shutdown circuit: CLOSED", and that pressing "Disable compressor" flips it to OPEN with the compressor row reading DISABLED; (2) confirm with a meter on the Q3 gate side of R22 that the pin really follows that readback — they only agree if `GPIO_MODE_INPUT_OUTPUT` took effect, and a plain output would read back 0 forever; (3) only then decide whether to connect the gate, which is the step that makes this able to actually fire the EBS. Until (3) the dashboard note on that page must keep saying so.

- [ ] **Stale gaps in kart-medulla's `.agents/esp32s3-pinmap.md`** — Noticed 2026-07-26 while closing gap #3 in that file. Gap #1 says "The S3 build does not exist. `platformio.ini` has only `esp32dev` and `native`", but that repo's `AGENTS.md` states `platformio.ini` now uses `[env:esp32-s3-devkitc-1]` as the primary target and the S3 build is what gets flashed. One of the two is wrong; `AGENTS.md` is the more recently maintained file, so gap #1 is the suspect. Check the actual `platformio.ini` and either delete gap #1 or correct `AGENTS.md`. While there, gap #5 (comms watchdog releases the brake instead of applying it) is still open and is now partly addressed — stale comms leave the shutdown chain open — so it should be reworded rather than left as written.

- [ ] **Activate and Validate ZED Object Tracking (Kalman Filter)** — In the ZED ROS 2 wrapper configuration (`src/kart_bringup/config/zed_overrides.yaml` or launch files), enable the `object_tracking` module. Currently, YOLO detections are independent frame-by-frame. Enabling this will allow the ZED SDK to apply its internal Kalman Filter to track cones, maintain their IDs, predict their position during dropped frames (glare/vibration), and filter out 1-frame ghost detections. Validation: Use RViz or `ros2 topic echo` to verify that cones on the `/perception/cones_3d` topic maintain a persistent ID number as the camera moves.

- [ ] **kart_sim sends no steering PWM, so the dashboard reads 0% in simulation** — Found 2026-07-18 while fixing the test suite. `encode_act_steering()` (`src/kb_dashboard/kb_dashboard/protocol.py`) builds a two-element payload `[angle x1000, raw_encoder]`, but `decode_steering_raw()` reads three, `[angle, raw_encoder, pid_out x1000]`, and the dashboard shows that third value as the PWM readout on the Telemetry page. The real firmware sends three; the ESP32 simulator at `src/kart_sim/scripts/esp32_sim_node.py:114` uses `encode_act_steering` and so sends two, leaving PWM permanently 0% in sim. Fix: give `encode_act_steering` a third `pid_pwm: float = 0.0` parameter appended to the payload (default keeps every existing caller working), then pass the simulated PID output from the sim node. Worth confirming first whether the sim models a PID output at all — if it does not, decide whether a plausible fake is better than a flat 0, since a reading that is always zero looks identical to a broken sensor.

- [ ] **Wire the EBS signals the dashboard is already waiting for** — The Race skin has an EBS tab (added 2026-07-18) whose tank/piston/compressor readouts are live, but whose **EBS state** and **valve** rows read `NOT WIRED` because nothing publishes them. The dashboard side is done: `protocol.py` defines `ebs_state` (`None | "unavailable" | "armed" | "activated"`, per FS 2026 T 14.8) and `ebs_valve_on` (`None | bool`), both defaulting to `None`, and the UI renders grey NOT WIRED for `None` — deliberately never green, so an unwired safety indicator can't read healthy. Work: (1) decide where the truth lives — the ESP32 knows the valve MOSFET state, the AS state machine knows whether the EBS has fired; (2) publish it (extend an ESP frame, or a ROS topic the dashboard node already subscribes to) and populate the two keys; (3) leave `None` for any signal genuinely absent rather than substituting a default. The EBS tab pulses red on `activated` or tank-out-of-band, so check it doesn't false-alarm once real values arrive. FS status inputs deliberately left out for now (ASMS, TS active, SDC open, R2D) — add them to this tab if the AS state machine needs debugging.

- [ ] **ESP32 health frame: add per-task stack minima** — The dashboard's System page has a "Stack" card (min free stack bytes across the comms/control/heartbeat/health tasks, red <200 B) but it always shows `--`: the `ESP_HEALTH_STATUS` payload is only 4 int32s `[flags, agc, heap_kb, i2c_errors]` and carries no stack data. Work: (1) in **kart-medulla**, extend the health frame with 4 more int32s from `uxTaskGetStackHighWaterMark()` per task → `[flags, agc, heap_kb, i2c_errors, stack_comms, stack_control, stack_heartbeat, stack_health]`; (2) in **kart-brain**, extend `decode_health()` (`src/kb_dashboard/kb_dashboard/protocol.py`) to emit `stack_*` keys when `len(payload) >= 8`, staying backwards-compatible with 4-int payloads. The UI already consumes the keys unchanged (Race-skin System card `rcSysStk` + legacy healthBar `STK`). Flash, then verify the card populates and turns red when a task is squeezed.

- [ ] **Rename remaining deployment folders to hyphenated names** — Partially done: `~/kart_brain` → `~/kart-brain` on the **Orin** was completed 2026-07-06 (dir moved, `.bashrc` + systemd unit updated, clean rebuild, verified running). Remaining:
    - **Orin:** `mv ~/kart_medulla ~/kart-medulla` + update any flash scripts/aliases referencing the old path (PlatformIO flash command in docs/memory uses `~/kart_medulla`).
    - **VM (`utm`):** check whether the workspace is still `~/kart_brain` and rename to `~/kart-brain` (+ `.bashrc`) if so.

- [ ] **Pure pursuit arrow/steering mismatch — still unverified after HUD refactor** — Symptom reported 2026-04-19: with `pure_pursuit` active, the dashboard green arrow pointed right while the physical steering wheel moved left. Commit `316b5cd` eliminated *one* possible cause by making the HUD draw its arrow from the controller's actual aim point (`/kart/target`, PointStamped) instead of the HUD's independent nearest-pair midpoint — so the arrow can no longer disagree with pure pursuit's target. Not yet re-tested on the kart after that change. If the symptom persists, the mismatch is between `cmd.angular.z` and the actuator (not between arrow and controller) — which should be impossible since the geometric controller works fine through the same `cmd_vel_bridge_node.py:67` path with no sign flip. Code review of `_control_pure_pursuit` (`src/kart_control/scripts/cone_follower_node.py:484-582`) found no sign bug; `atan2(target_y, target_x)` uses the same `left` convention as geometric. Suspects if the problem returns: (a) far-lookahead target on the opposite side of a curve entry, (b) positive-feedback adaptive lookahead loop using `_last_steer` at line ~552, (c) `steering_gain=3.0` multiplying PP's already-valid angle into ±`max_steer` saturation (line ~570), (d) wide-straight cross-pairing with the 6 m cutoff (line ~527). Diagnostic: log `self._last_target` alongside nearest-pair midpoint on each frame, compare. Do NOT just disable pure pursuit — the user wants to use it, and the HUD fix may already be sufficient.
- [ ] **Validate ZED neural net inference via TensorRT** — Build ZED SDK with TensorRT support (check submodules), test that the custom YOLO model runs through ZED's built-in neural detection module instead of a separate Python node. Compare latency vs current ultralytics pipeline.
- [ ] **Benchmark C/C++ vs Python for YOLO node** — The current `yolo_detector_node.py` runs inference in Python (ultralytics). Investigate whether a C++ ROS2 node using TensorRT C API or ZED SDK's detection API gives meaningful FPS improvement. Check: inference time, pre/postprocessing overhead, GIL contention.
- [ ] **Per-cone depth: confirm and document the fast path** — Two perception launch files exist: (a) `perception_3d.launch.py` runs `yolo_detector_node` + `cone_depth_localizer_node` and consumes the **full** depth image from the ZED ROS 2 wrapper (`/zed/zed_node/depth/depth_registered`), then samples it at YOLO bounding-box centers; (b) `perception_zed_od.launch.py` uses the ZED SDK's built-in object-detection module (publishes `ObjectsStamped`, consumed via `zed_od_utils.zed_objects_to_det3d`), where the SDK runs the custom YOLO model on-device and returns 3D bounding boxes directly — the full depth image is never published to ROS in this path (commit `4b4fc0b` "Add ZED SDK built-in object detection support with custom YOLO model"). Path (b) is the actual fast path running on the kart. Two open questions: (1) is path (a) still used anywhere, or can `cone_depth_localizer_node.py` and `yolo_detector_node.py` be retired? (2) Does the SDK's object-detection path internally compute a full depth pass anyway and just hide it from us, or is it really only computing depth at detected objects? Related to the existing task at line 10 ("Validate ZED neural-net inference via TensorRT"). Document whichever path is canonical in `architecture.md` so future content posts don't have to re-derive it.

- [ ] **In-person verify: LAN-IP dashboard login after Secure-cookie fix** — Fix committed in `31d4bb7` on 2026-04-20 makes the session cookie's `Secure` flag conditional on `X-Forwarded-Proto: https` from a loopback peer (cloudflared). Not yet tested on-kart because Orin is off when Ruben is at home. Next time at the kart, after `git pull` + `sudo systemctl restart kart-brain` on Orin: (1) visit `http://<orin-lan-ip>:9090` from a phone on the same Wi-Fi, type password `0`, confirm the dashboard actually loads (not the login page again); (2) visit `https://kart.rubenayla.xyz`, confirm login still works there (regression check); (3) in Chrome/Safari devtools, inspect the `kart_session` cookie — LAN path should show `Secure: false`, Cloudflare path `Secure: true`. Close the task only after both paths are confirmed.

- [ ] **Enable `http://kart` memorable dashboard URL (dnsmasq name on the AP)** — The dashboard now binds port 80 (commit `91a8026`), so `http://10.42.0.1` works without `:9090`. Remaining: make the bare name `kart` resolve on the AP so users can type `http://kart`. On the Orin, when up on the kart: `echo 'address=/kart/10.42.0.1' | sudo tee /etc/NetworkManager/dnsmasq-shared.d/kart-name.conf` then `sudo nmcli connection down kart-ap && sudo nmcli connection up kart-ap` (drops AP clients ~5 s while dnsmasq reloads). First verify the running dnsmasq uses `--conf-dir=/etc/NetworkManager/dnsmasq-shared.d` (`ps aux | grep dnsmasq`); if not, put the line in the config it actually reads. Verify from a phone on the `kart` Wi-Fi that `http://kart` loads the dashboard. Details: `.agents/orin-environment.md:68-75`.

- [ ] **`control_task` stalls ~1.1 s then fires 10 iterations back-to-back — the steering PID must run at 500 Hz or better** — Root-caused 2026-07-25. The task averages 8.9 Hz, but the average is a lie: measured at the serial line, steering frames arrive in **bursts of 10 with 0.0 ms between them, separated by 1102 ms gaps** (8 such gaps, 81 zero-gaps, in a 10 s capture). Two causes compound. (1) The AS5600 read blocks for ~1.1 s because nothing is on the I2C bus — that part is expected while the steering sensor migrates to the **MT6701 read as PWM on GPIO 1**, and is not itself the bug to fix here. (2) `KM_RTOS`'s wrapper (`components/km_rtos/km_rtos.c:259`) uses `vTaskDelayUntil(&xLastWakeTime, pdMS_TO_TICKS(period_ms))`, which after an overrun returns *immediately, repeatedly*, until `xLastWakeTime` catches up to real time — turning any stall into a catch-up burst. **Cause 2 outlives cause 1**: even with a fast sensor, any transient hiccup produces a burst of PID steps with dt~0, which is worse for a controller than running evenly slow. Work: (a) finish the MT6701 MCPWM-capture path so the sensor read is non-blocking, or failing that give the I2C read a few-millisecond timeout instead of ~1 s; (b) make the wrapper resync `xLastWakeTime = xTaskGetTickCount()` after a detected overrun rather than replaying missed periods; (c) move the compressor and pneumatics out of this task into their own slow task — they need ~1 Hz, the steering needs 500 Hz+, and coupling them means the compressor's requirements ride on the PID's task. Then verify the true rate with the `control_iters` counter in the `ESP_PNEUMATIC` frame, not with frame arrival rate. **The 0.88 Hz pneumatic frame rate is explained by this and is not a separate bug** — the wall-clock throttle correctly sees ~0 ms elapsed inside a burst, so it emits once per burst.
    - **Re-measured 2026-07-30 on the kart: the 1.1 s stall is gone.** `ros2 topic hz /esp32/steering` over a 10000-sample window gave **477.1 Hz, min gap 0.000 s, max gap 0.014 s, std dev 0.0030 s** (kart power off, Orin and ESP32 only, firmware at kart-medulla `00e7469`). A 1.1 s gap would have shown up as `max` — it did not, across 10000 samples. The dashboard's own ESP32-link readout agrees, showing 500.0 Hz. So the symptom that opened this task no longer reproduces, and the 8.9 Hz figure above is historical.
    - **What that does and does not settle.** It does not confirm any of (a), (b) or (c) were done — the likely explanation is that the MT6701 PWM path replaced the blocking AS5600 I2C read, removing cause 1, and nobody has checked whether cause 2 (`vTaskDelayUntil` replaying missed periods after an overrun, `components/km_rtos/km_rtos.c:259`) was ever touched. That latent bug turns any future stall back into a catch-up burst, so it is still worth fixing on its own merits. Item (c), moving the compressor and pneumatics out of the steering task, is also still undone.
    - **The residual jitter is real and worth a look before tuning.** At a 2 ms period the gaps should cluster near 2 ms, but `min` is 0.000 s and `max` is 14 ms with a 3 ms standard deviation — so frames still arrive somewhat bunched. That is a much milder version of the same catch-up signature, which is consistent with cause 2 being untouched. **This matters for PID work:** a derivative term behaves completely differently at dt = 2 ms than at dt = 14 ms, and worse when dt is occasionally 0. Anyone tuning kp/ki/kd should know the loop rate is fast but not evenly spaced.

- [ ] **kart-medulla: the S3 build silently links stale object files** — Found 2026-07-25 while chasing a compressor that would not run. `components/km_gpio/km_gpio.c` was last modified that day, but `.pio/build/esp32-s3-devkitc-1/components/km_gpio/km_gpio.o` and `esp-idf/km_gpio/libkm_gpio.a` were both dated **jul 18 12:04**, and `touch`-ing the source did **not** trigger a rebuild — `pio run` reported SUCCESS having recompiled nothing. Deleting the two artefacts by hand forced a correct recompile, which then succeeded, so the source is fine; the dependency tracking is not. **This is dangerous well beyond the compressor: any edit to a `components/` source can appear to build and flash while the firmware actually keeps week-old behaviour, which makes every on-hardware test a potential lie.** It did not cause the compressor symptom (the compressor code landed 07-16, before the stale object was built 07-18) but it could easily hide the next fix. Work: find why the CMake/ninja dependency graph misses `components/*` sources in this PlatformIO+ESP-IDF setup — note the objects land in a non-standard `.pio/build/<env>/components/km_gpio/km_gpio.o` rather than the usual `esp-idf/<comp>/CMakeFiles/__idf_<comp>.dir/` path, which is itself a clue. Until it is fixed, **`rm -rf .pio/build/esp32-s3-devkitc-1` before any build whose result you intend to trust.**

- [ ] **PRESSURE_2 needs a sensor fitted and a calibration** — Found 2026-07-25. The channel reaches the dashboard end to end (firmware reads GPIO 7 / ADC1_CH6, appends it to `ESP_PNEUMATIC`, `decode_pneumatic` converts it, the Race skin's PISTON dial renders it — a 6 px bar until 2026-07-30, a dial since), but no sensor is connected, so the input floats and reads a railed 4095. **The false-reading half is fixed**: a channel pinned at full scale now decodes to `None` and shows `-- bar` rather than the confident "9.9 bar" it displayed before. **2026-07-30: something is now wired to PRESSURE_2 — Ruben confirmed a wire is physically connected but does not know what it is; untested, so treat any reading from that channel as unidentified until he checks it.** Measured live at 12:48 that day: PRESSURE_2 reads exactly 0 raw counts and 0 mV, i.e. sitting at ground rather than floating at the rail, so the wire carries no signal yet. PRESSURE_1 in the same frames read 1143-1164 counts / 992-1012 mV = ~3.0 bar, having shown 0.0 bar an hour earlier. **What made PRESSURE_1 start reading is unresolved** — no commit since 2026-07-29 touches the pressure decode, so it is not a software change; the ESP32 had rebooted 7.5 min before (heartbeat uptime 448111 ms, boot ~12:41) and the candidates are the SDE5's 24 V supply coming on, real air entering the reservoir, or the reboot re-initialising the ADC. Ask Ruben which, and record it here. Remaining: (1) identify what is on PRESSURE_2, then fit/confirm the piston/brake-line sensor and confirm the reading tracks real pressure; (2) **calibrate it** — the decode reuses PRESSURE_1's ÷3 divider ratio purely on the assumption the channels are identical, and unlike PRESSURE_1 (gauge-anchored 2026-07-12) it has never been checked against anything. Note PRESSURE_1's own calibration is disputed: the gauge-anchored map and the `bar = 3 x Vadc` map disagree by about a bar on the same ADC count, documented in kart-medulla `main/main.c`. Resolve both against one gauge in the same session.

- [ ] **Audit the dashboard for other sensors that can render a false reading when absent** — Two were found and fixed on 2026-07-25, both discovered only by looking at the screen with the hardware unplugged: the steering gauge showed a confident **90 LEFT** (firmware reports 3.451 rad with nothing on I2C; the gauge clamped it to its -90 limit) and the PISTON readout showed **9.9 bar** (floating ADC input at full scale; it was a bar widget then and is a dial since 2026-07-30). The standing rule is that an absent or out-of-range sensor must read as no-data — `--`, `NO SENSOR`, `NaN` — and must never produce a plausible number, because a false reading is indistinguishable from a real one and will be trusted. Sweep the rest of the panels the same way: speed, the accelerometer/G-G dial, pedal positions, battery, YOLO Hz, and anything else that defaults a missing value to 0 or clamps an out-of-range one into scale. The EBS tab's existing `NOT WIRED` treatment is the pattern to copy. Check by disconnecting or stubbing each source, not by reading the code.

- [ ] **Two things about the Wi-Fi fallback are still untested. Both need someone at the kart.**

    Background in one line: when the phone is plugged in over USB the Orin runs the `kart` Wi-Fi; when the phone is gone it drops `kart` and joins a known Wi-Fi instead, so you can still reach it remotely. Done by `tools/wifi-watchdog.sh`.

    **Test A — no known Wi-Fi in range.** This is the one that matters, because if it fails the kart is left with no `kart` network *and* no internet, which is worse than before.
    - Go somewhere with no known Wi-Fi, or turn off every phone hotspot and be out of range of `Robots_urjc`.
    - Unplug the USB phone from the Orin.
    - Wait 40 seconds.
    - **Pass:** the `kart` Wi-Fi is back and `http://10.42.0.1/` loads. **Fail:** no `kart` network appears at all.

    **Test B — the tether self-heal.** Not deployed yet; deploy first with `scp tools/wifi-watchdog.sh orin-remote:/tmp/ && ssh orin-remote 'sudo install -m755 /tmp/wifi-watchdog.sh /usr/local/bin/ && sudo systemctl restart wifi-watchdog'`.
    - With the phone plugged in and working, run `sudo nmcli connection down "Wired connection 2"` on the Orin. This kills its internet.
    - **Pass:** within about 2 minutes the Orin brings the phone connection back by itself and `kart.rubenayla.xyz` works again. **Fail:** it stays dead until you replug the cable.

    **Before either test, check the Orin is actually on:** `ssh orin-remote 'uptime'` must print real output. When Rubén is at home the kart is off, and a powered-off Orin looks exactly like a failed test.


**Result of the 2026-07-31 run, using the procedure above: still 530 after 3.2 minutes** (18 polls, 21:52:11 to 21:55:05, every 10 s). Which of the two failure modes that was had not yet been read off the Orin's journal when the run ended. Start there rather than re-running the test.


**A mode-switch cannot look before it leaps — scanning while in AP mode returns nothing, measured on the Orin 2026-07-30.** The appealing safety feature would be to check whether any known network is actually in range *before* taking the `kart` AP down, so the AP only ever drops when the switch is going to succeed. That is not available on this radio. With `wlP1p1s0` in `type AP` serving `kart`, `nmcli -f SSID,SIGNAL dev wifi list --rescan yes` returned exactly one row — `kart` itself, signal 0 — and no other network, in a room where `Robots_urjc` and phone hotspots are normally visible. A direct `sudo iw dev wlP1p1s0 scan` never returned at all: it was still running after 2 minutes and had to be killed by PID. (`pkill -f "iw dev wlP1p1s0 scan"` is the wrong way to kill it — the pattern matches the killing shell's own command line, so the shell kills itself and the SSH session dies with exit 255.) The AP kept serving `http://10.42.0.1/` with HTTP 200 throughout both attempts, so probing costs nothing, but it also tells us nothing. Consequence for the design below: every fallback attempt is blind and must drop the AP first to find out, so each attempt costs a real ~30 s window with no `kart` network. That is the argument against any repeating trigger — one attempt per discrete cable-unplug event, never a polling timer, or the kart loses its dashboard for 30 s out of every poll interval while parked somewhere with no known network.

**Option A — switch the single radio between AP and client mode on cable events. Free, and worth trying before spending anything.** Sequential mode-switching needs no new hardware and is what the Orin did before 2026-07-06, when it joined `Ruben's iPhone` at priority 100. Make it event-driven, never a blind timer: a NetworkManager dispatcher script on `enxfe9ca7a9ecdb` going **down** brings `kart-ap` down and lets the client profiles autoconnect on the priorities already set; if nothing associates within about 30 s it **must bring `kart-ap` back up**, because with neither AP nor client there is no dashboard at all and a bad guess at the track would leave the kart blind. On cable **up**, return to AP so the dashboard sits at a stable `10.42.0.1` whenever the tether is plugged. Two costs to accept: in client mode the `kart` network does not exist, so anyone on it is dropped and the dashboard address becomes whatever the joined network assigns (findable via Cloudflare or the hotspot's client list, but no longer fixed); and the obvious fallback network is the same phone that was meant to be plugged in, so this covers "cable unplugged or broken, phone in pocket" but not "phone absent".

**Option B — add a USB Wi-Fi dongle (~€10) as a second radio.** Strictly better behaviour, at the cost of money and a driver risk: built-in radio keeps beaconing `kart` continuously, dongle joins known networks as a client, no mode-switch state machine, no drop-outs, dashboard address never moves. Pick a chipset with in-tree support for JetPack 6 (kernel 5.15, aarch64) — driver availability is what bites on Jetson, so do not buy on price alone.

**How to test whichever option is built — three cases, and the third is the one that matters.** The test severs the tester's own remote access, because `ssh orin-remote` and `kart.rubenayla.xyz` both ride the Cloudflare tunnel over the tether being unplugged. So instrument first and read the record afterwards rather than trying to watch live: `nohup` a script that appends `nmcli -t -f DEVICE,TYPE,STATE,CONNECTION device`, `nmcli -t -f CONNECTIVITY general`, `iw dev wlP1p1s0 info` and `ip route | grep default` to a log file every 5 s, then pull the cable. Keep a laptop or phone joined to the `kart` Wi-Fi (pwd `umotorsport`) as the local way back in at `ssh orin-local` / `http://10.42.0.1/`, and note `tcpdump` is **not** installed on the Orin if packet-level detail is ever needed. Cases: (1) **known network in range** — unplug with a phone hotspot on and reachable; pass = a client profile associates within ~30 s, the hotspot shows a connected device, `ip route` regains a default via Wi-Fi, and the dashboard answers on the address that network assigned. (2) **cable back** — replug; pass = the default route returns to the tether (metric 100 beats Wi-Fi's 600) and the `kart` AP is back up, so `http://10.42.0.1/` works again. (3) **no known network in range** — unplug with every known hotspot off or out of range; **pass = `kart-ap` comes back up within ~30 s and the dashboard is reachable at `10.42.0.1` again.** Case 3 is the whole safety argument for the design and the easiest to skip, since cases 1 and 2 both look fine while it is broken; a mode-switch that strands the kart with no AP and no client is worse than having no fallback at all. Verify each from the log plus a real dashboard load, not from `nmcli` alone.

Either way the desired priority order — USB tether first, then the phone hotspots, then `Robots_urjc` — needs no new config: the tether's DHCP default route already has **metric 100 against Wi-Fi's 600**, so USB wins automatically whenever it is plugged, and the Wi-Fi profile priorities above already encode the rest. Keep `kart-ap` as the resting state with autoconnect priority above every client profile, so a reboot at the track always comes up with `kart` present. Context: `history.md` 2026-04-20 "option 2", 2026-07-25 and 2026-07-26, `.agents/orin-environment.md` "Network architecture".

- [ ] **Wire the pneumatic pressure sensors (tank + piston) end-to-end** — The Race-skin **EBS** page has a Pneumatics panel (`index.html`): two side-by-side dials, TANK (`RC_DIAL_TANK`, green 6-10 bar safe band) and PISTON (`RC_DIAL_PIST`, no bands). They read `d.pneu_tank_bar` / `d.pneu_piston_bar`. **This entry's claim that "no firmware sends yet" is stale and contradicts the PRESSURE_2 entry above**, which says both channels reach the dashboard end to end as of 2026-07-25 — check which is true against live frames before acting on either. What remains for certain is the piston sensor itself (not fitted) and its calibration. Work: (1) **kart-medulla**: read the pressure sensors and transmit them (new `ESP_*` frame ids, or extend an existing health/braking frame). The tank sensor is a **Festo SDE5-D10 (567465), 0-10 bar** (analog); wire it to an ADC. **Verified 2026-07-12 (bench):** tank sensor = `PRESSURE_1` on **CN7.1 → ESP32-S3 GPIO 6 (ADC1_CH5)**; SDE5 outputs 0-10 V (1 V/bar) and the on-board divider (R11+R12 10k+10k series / R13 10k to GND) scales it ÷3, so **`bar = 3.0 * V_adc`** (use `analogReadMilliVolts(6)`; needs the SDE5's ~24 V supply powered — see `~/dv/tasks.md` "24 V supply for the SDE5"). Bench reader firmware proven in the scratchpad comp_test PlatformIO project. (2) **kart-brain**: add decode in `protocol.py`, subscriptions in `dashboard_node.py`, and `pneu_tank_bar` / `pneu_piston_bar` to `DashboardState`. Limits are already set from the real hardware — **Festo CRVZS-0.75 (160235) reservoir, operating 6-10 bar** (`PNEU_TANK_MIN=6, PNEU_TANK_MAX=10, PNEU_MAX_BAR=10`); below 6 bar makes the EBS tab pulse red (the System tab deliberately excludes tank pressure, so one fault pulses one tab). Details + datasheets: `~/dv/kart/pneumatics/README.md`. Note: the **piston pressure is the brake-pressure sensor**, so the same sensor should also feed the pedals dial's red "real" brake needle (`rcDrawPedals`, currently mapped to `esp32_braking`) — unify them: one sensor drives both the piston dial here and the brake-real needle on the TELE page. Throttle stays a single needle (no independent throttle sensor; it is the commanded electronic effort).
    - **TANK half implemented 2026-07-17 (not yet Orin-built/validated — leave unchecked).** kart-medulla now sends a dedicated `ESP_PNEUMATIC` (0x0C) frame `[tank_pressure_adc, compressor_duty]` at ~20 Hz (firmware on `dev`). kart-brain side wired here: `decode_pneumatic` in `protocol.py`, `/esp32/pneumatic` sub + `_on_esp_pneumatic` in `dashboard_node.py`, `esp_pneumatic_pub_` in `kb_coms_micro` (hpp/cpp/Frame.msg tag), and `pneu_tank_bar` now feeds the TANK dial. Also added beyond the original scope: a **compressor on/off + duty bar** in the Pneumatics panel (`esp32_compressor_on` / `esp32_compressor_duty`), since the EBS compressor now soft-starts and its state is worth watching. Firmware sends **raw ADC** and `protocol.py` converts with the verified `bar = 3.0 * V_adc` (linear ADC model — see the accuracy note in that file). **Still TODO:** `colcon build` on the Orin (Mac has no ROS2, so the C++ publisher is unbuilt), confirm `/esp32/pneumatic` publishes and the dial moves; then the **PISTON** half (brake-pressure sensor) is still entirely unstarted.
    - **Pre-existing `test_decode.py` failures, unrelated (found 2026-07-17):** on a clean `dev`, 3 fail — `TestDecodeSteeringRaw` unpacks 2 values but `decode_steering_raw` now returns 3, and `TestMissions` is missing `autonomous`. Not caused by the pneumatic work; someone should resync those tests.

- [ ] **Future dashboard telemetry inputs — hydraulic brake pressure + battery BMS voltage** — Two data sources planned but not wired yet; add them to the dashboard as placeholders (fields default to `--`, `?demo=1` can fill them) so it's ready:
    - **Hydraulic brake-line pressure sensors** — going on the brake lines (in addition to the pneumatic piston-pressure work). When they land, add `hydraulic_pressure_*` fields to `DashboardState`/`protocol.py`/`dashboard_node.py` and surface them (likely next to the pneumatics panel or as brake-real needles). Stub commented-out / placeholder until the sensors exist.
    - **Battery voltage/SOC from the JBD (Jiabaida) BMS** — same JBD protocol (read "Basic Info" reg 0x03 → total voltage in 10 mV units, SOC, cell voltages, temps); open parsers exist (ESPHome `jbd_bms`, esp32-xiaoxiang-bms, bms2mqtt). **Preferred path: read it from the Orin over BLE** — the battery is far from the ESP32 (cabling UART is impractical), and the Orin already runs the dashboard/ROS and has Bluetooth. Use Python `bleak`: connect to the JBD BLE service (`ff00`, write commands to `ff02`, receive notifications on `ff01`; reassemble the multi-packet response to the `0x77` end byte), poll ~1 Hz, publish to a ROS topic → dashboard shows a real `battery_voltage`/`battery_soc` (replaces the removed "battery %" placeholder). Caveats: JBD BLE allows **one connection at a time** (app OR reader, not both); verify the Orin's BT works (`bluetoothctl list`). Fallback: ESP32 reads it (UART with the onboard-BT-shares-the-port gotcha, or ESP32-BLE — adds the BLE stack to the control MCU).

- [ ] **[Workshop] Verify the JBD BMS connects over BLE and streams data** — Hands-on check next time at the kart. The dashboard already has a **Battery card** (System page, `rcSysBat`/`rcSysBatSoc`) showing `--` until real data arrives. Steps: (1) on the Orin, `bluetoothctl` — scan and confirm the JBD BMS advertises and is connectable (BT works, one-connection-at-a-time; disconnect the phone app first). (2) Quick Python `bleak` probe: connect to the JBD BLE service (`ff00`), enable notifications on `ff01`, write `DD A5 03 00 FF FD 77` to `ff02`, reassemble the multi-packet reply to the `0x77` end byte, parse total voltage (2 bytes ×10 mV) + SOC. (3) Confirm the numbers match a multimeter reading of the pack. (4) If it works: wire it into a small ROS node publishing `battery_voltage`/`battery_soc`, feed `DashboardState`, and confirm the Battery card lights up live (not `--`). Reader-vs-app coexistence: only one BLE client at a time — plan a "release BMS for app" pause on the reader (dashboard toggle) if both are ever needed.

- [ ] **(at the kart) Verify the five POWER OFF feedback screens on real hardware** — Added 2026-07-30, kart-brain commit `ec5ea29`. The dashboard's POWER OFF button used to show nothing at all after the confirmation dialog; it now paints a full-screen overlay that names which of five outcomes happened. Only the failure paths were exercised on the Mac, against a local server with a stubbed shutdown command — none of it has run against a real Orin, and the two paths that matter most (the machine actually going down, and the browser reporting it) cannot be faked convincingly. Verify at the kart, on a phone rather than a laptop, since the overlay is `position:fixed` and the Race skin runs fullscreen in landscape:
    1. **Orin is off** (the main one) — press POWER OFF, confirm the dialog. Expect "Powering off / accepted, ~3 seconds", then within a few seconds a green **ORIN IS OFF**. It must *not* fall back to the reconnect churn or a blank page. Then press Dismiss and power the Orin back on physically: the page should reconnect on its own without a reload.
    2. **Not sent** — pull the Wi-Fi (walk out of AP range or turn the phone's Wi-Fi off) so the connection dot goes dark, then press POWER OFF. Expect an amber **NOT SENT**, and confirm afterwards over SSH that the Orin never rebooted.
    3. **Power off failed** — hard to trigger deliberately; if `sudo` ever refuses, the screen should show the stderr line verbatim (e.g. `sudo: a password is required`). Worth checking `journalctl -u kart-brain` for an `Orin poweroff failed:` line if the shutdown ever silently doesn't happen.
    4. **Still running** — if the Orin is still answering 25 s after accepting, expect an amber **STILL RUNNING**. This is the safety net for a shutdown that hangs.
    5. **Demo mode** — open the dashboard with `?demo` and press the button; it must say demo mode and send nothing.
    Also check the overlay is legible in daylight and that Dismiss is reachable in the landscape Race skin. Close this task only after step 1 has been seen on the actual kart.

## In Progress

### Code shipped, awaiting on-kart validation — branch `feature/imu-corrected-ground-plane`

Both items below are coded, committed, pushed (commits `71dbdeb`, `6c50400`, `cec790d`). The remaining work is at the workshop. Boot order on the Orin:

```bash
git fetch && git checkout feature/imu-corrected-ground-plane
colcon build --packages-select kart_perception kb_dashboard --symlink-install
source install/setup.bash
# Terminal 1
ros2 launch kart_perception perception_zed_od_ground.launch.py
# Terminal 2
ros2 launch kb_dashboard dashboard.launch.py
# Browser
http://<orin-ip>:9090
```

- [ ] **(workshop) Validate the IMU-corrected ground plane** — `ground_plane_localizer_node` subscribes to `/zed/zed_node/imu/data` and `/zed/zed_node/obj_det/objects`, applies a swing-twist correction (strips yaw about gravity, keeps the residual pitch+roll), and republishes cones on `/perception/cones_3d_ground`. Validation:
    1. **Static, level kart** — `/perception/cones_3d_markers` (raw, in optical frame) and `/perception/cones_3d_ground_markers` (corrected, cyan/orange shades) should overlap exactly in RViz. If they diverge at rest, math/convention is off. First fix to try: set `invert_correction:=true` in the launch params. Second: change `gravity_axis` from `[0,0,1]` to whatever the wrapper actually publishes against.
    2. **Hand-tilt the camera ±10°** — original markers swing forward/back with the tilt; ground-corrected markers should stay roughly fixed in space. If they swing the same direction as the originals, flip `invert_correction`.
    3. **Drive forward + slam the brakes** — original cone distances will drop systematically (kart pitches forward → camera looks down → cones look closer). Corrected positions should hold roughly constant. Record a rosbag of this run — it's the receipt for the LinkedIn dashboard post and the migration evidence for the MPC controller.
    4. **Sanity-check the IMU bias under sustained braking** — `ros2 topic echo /zed/zed_node/imu/data --field orientation` during the slam-brake test. If the quaternion swings noticeably more than the kart's actual pitch, the ZED's stock fusion is biased by linear deceleration. <30% extra: ship as-is. >50% extra: file follow-up to add gyro-dominant complementary filter weighting.
    - Only after the first three pass, mark this Done and start a separate PR migrating `cone_follower_node.py` (the MPC) to consume `/perception/cones_3d_ground`.
    - Rosbag to record at the workshop:
      ```bash
      ros2 bag record -o ground_plane_validation \
        /zed/zed_node/imu/data \
        /zed/zed_node/obj_det/objects \
        /perception/cones_3d \
        /perception/cones_3d_ground
      ```

- [ ] **(workshop) Validate the top-down (cenital) cone panel in the dashboard** — Default skin now shows a "Perception — Top-down" card with cones from `/perception/cones_3d_ground` rendered as colored dots, kart at the bottom, range rings at 5/10/15 m. Validation:
    1. **Empty FOV** — only the range rings, heading line, and white kart triangle. No stray dots.
    2. **One cone at ~3 m, slightly right** — should appear ~⅕ of the way up the canvas, slightly right of center. Color should match the cone's class (blue/yellow/orange/large-orange).
    3. **Cone wall at known 5 m / 10 m / 15 m** — dots should land near the corresponding range ring.
    4. **Tilt the camera by hand** — dots should NOT move. If they do, the IMU correction (above task) isn't doing its job; that's the failure to debug, not the panel.
    5. **Wrong direction?** If cones appear *behind* the kart (below the canvas) or mirrored left-right, edit the `CENITAL_*_SIGN` / `CENITAL_*_AXIS` constants at the top of `drawCenital` in `src/kb_dashboard/kb_dashboard/index.html` and reload the browser. No rebuild — `index.html` is served fresh on each page load.
    - Mark Done when the static + cone-wall + tilt tests all behave as expected on at least one phone or laptop.

## Blocked

## Done
