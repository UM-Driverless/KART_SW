<!-- reference — read only when you need the history of a shipped item -->
# Done archive — completed work items

Closed items moved out of the root `tasks.md` on 2026-08-10, following the same convention as
the partle and kart-medulla repos. Nothing here is actionable: the root board carries only live
work, while the reasoning behind finished things stays findable.

The board is `tasks.md` at the repo root — the only task board in this repo.

## Closed items from the board


### Ready

- [x 2026-08-10] **The gamepad drives the kart with no visible UI, and the Angle/PWM steer toggle is unreachable** — Fixed in `6aab151`. `#manualWidget` is no longer hidden by the race skin's `display:none !important`; it is restyled to the skin and relocated into the Remote control pane under the touch pad (`#rcGpSlot`), the same way the pad itself is relocated in `skinRace.init`, and restored to its home on skin switch. Its show/hide and the polling loop's send now key off the same `currentMission === 'remote_control'` test, so the block can never be hidden while the loop is still driving. The Angle/PWM buttons became a labelled pair, "Angle (PID holds it)" / "Raw PWM (no PID)", selected via a class rather than inline styles. Brake left, accel right, matching the pedals dial. Verified in a browser with a stubbed gamepad: status reads the controller name in green, the steering bar tracks axis 0 (-0.62 drew left of centre), the trigger bars read 30 % brake and 85 % accel, and clicking Raw PWM swapped the selection and sent `{action:'set_steer_mode', mode:'pwm'}`. Not yet exercised with a real controller on the kart. Original write-up follows.
    - Found 2026-07-30. `#manualWidget` in `src/kb_dashboard/kb_dashboard/index.html` is hidden by `#dbgConsole,#manualWidget{ display:none !important }` in the race-skin CSS, but it is not inert: `updateGamepadUI()` runs on a 10 Hz `setInterval` and, whenever the mission is `remote_control`, reads a connected gamepad and calls `wsSend({action:'manual_control', ...})`. So a USB gamepad steers the kart right now while its status readout, steering bar and throttle/brake bars render nowhere — a control with no feedback. Hidden inside the same element are `btnSteerAngle` / `btnSteerPWM`, which set `manualSteerType` and send `set_steer_mode` (ORIN_STEER_MODE, PID angle vs direct PWM); that firmware control currently has **no reachable button anywhere in the UI**. Fix: give the gamepad connection status, its axis bars, and the Angle/PWM toggle a home in the race skin's Remote control pane, next to the touch joystick and the Steering PID panel, then delete `#manualWidget`. Do not delete the markup without rehoming them — that leaves the gamepad driving blind — and do not delete `updateGamepadUI` without deciding to drop gamepad support outright, which is a separate call.

- [x 2026-07-30] **Telemetry page: the boundary between the YOLO-rate dial and the G-force dial twitches every frame** — Fixed and confirmed on the kart by Rubén. Cause was as diagnosed: `.rc-mini` grid items defaulted to `min-width:auto`, so the `1fr` track could not shrink below the nowrap g-force caption, whose width changed as the minus signs came and went. `min-width:0` on `.rc-mini` pins the tracks at 50/50. The caption was separately still wider than its dial, so it now stacks onto two rows (`LON x.xx g` / `LAT x.xx g`), measured at 43 px against a 91 px dial. Commits `cc59be4` and `21f82b3`.

- [x 2026-07-31] **Does the Cloudflare tunnel work when the Orin's internet comes from Wi-Fi rather than the USB tether?** — Yes. Confirmed by Rubén from experience: this was the normal arrangement for months before the `kart` access point became the default operating mode on 2026-07-06, when the Orin simply joined a Wi-Fi network as a client and `cloudflared` ran over it. No test needed, and none should be scheduled. Note this answers only the tunnel question — whether the radio *switches* from AP to client when the tether disappears is separate, and is what `tools/wifi-watchdog.sh` handles.

- [x] **Give the Orin an internet fallback for when the USB tether is gone — IMPLEMENTED 2026-07-30 as a rewritten `wifi-watchdog`** — Option A below was built: `tools/wifi-watchdog.sh` and `tools/wifi-watchdog.service` in this repo, installed to `/usr/local/bin/` and `/etc/systemd/system/`. It serves the `kart` AP while the tether is present and releases the radio to join a known network when the tether goes away, never dropping the AP while a station is associated, and spending one attempt per unplug event rather than retrying on a timer. Verified on the kart: a real cable unplug at 15:05:01 released the AP and joined `Robots_urjc` by 15:05:08, and replugging returned it to the AP. The lab network gives real internet (`10.7.20.142/24`, ping 1.1.1.1 at 22–35 ms, HTTPS 200, `generate_204` → 204, no captive portal). Design rationale and the gotchas live in `.agents/orin-environment.md`. **The root cause of there being no fallback before was not a missing feature but a broken one:** the previous `/usr/local/bin/wifi-watchdog.sh` ran `nmcli device connect wlP1p1s0 || nmcli connection up "iPhone de JBA" || nmcli connection up "Robots_urjc"` every 5 s, and the first command always succeeds by bringing up `kart-ap` (autoconnect-priority 200), so the `||` branches were unreachable. It also made the AP look like it had an unkillable autoconnect, because it yanked the device back mid-association on any manual attempt to join a network. Backup of the old script: `/usr/local/bin/wifi-watchdog.sh.bak-20260730`. What remains is only the tunnel question in the task above. Original write-up follows.
    - **(Superseded — kept as the pre-fix record. A Wi-Fi fallback exists and works: `wifi-watchdog` has joined `Robots_urjc` 7 times, and a cold boot with no tether found a network in 19 s on 2026-08-10. The hotspot is required for the *USB tether* to exist at all, because iOS only exposes the USB ethernet function when Personal Hotspot is on — not for the Orin to reach the internet. What still needs the tether is serving `kart` at a fixed `10.42.0.1` *while* having internet, which one radio cannot do as AP and client at once.)** Today the only way the Orin reaches the internet (and therefore the only way `kart.rubenayla.xyz` and `ssh orin-remote` work) is an iPhone plugged in over USB with Personal Hotspot on. **There is no Wi-Fi fallback and there cannot be one on the built-in radio: the free alternative was checked and ruled out on 2026-07-26, twice over.** First from the driver side — `iw list` on the RTL8822CE prints **no "valid interface combinations" section at all** (196 lines of output, none of them a combinations block), so AP+STA concurrency is not advertised. Then confirmed on the hardware: with the USB cable physically unplugged for 2m 07s while a 5-second-interval logger ran, `wlP1p1s0` stayed `connected:kart-ap` in `type AP` mode for all 112 samples, `type managed` never appeared once, the default route was `NONE` for the whole window, and NM connectivity sat at `limited`. NetworkManager never even attempted a client association — independently confirmed from the phone side, where the hotspot showed no connected-device indicator. The `kart` AP kept serving the local dashboard at `http://10.42.0.1/` throughout, so an outage costs only internet (`kart.rubenayla.xyz`, `ssh orin-remote`), never the trackside dashboard. It is currently `type AP` on channel 1, and `kart-ap` holds autoconnect-priority 200, so the existing client profiles (`Ruben's iPhone` 100, `8=======D` 90, `iPhone de JBA` 50, `Robots_urjc` 10) can never activate. Read that ruling narrowly: what is impossible on one radio is AP **and** client *simultaneously*.


## Previously under the board's `## Done` heading


- [2026-04-20] **Dashboard login works on LAN IP (plain HTTP)** — Session cookie `Secure` flag is now conditional on `X-Forwarded-Proto: https`, which cloudflared stamps on tunnel-forwarded requests. Only trusted when the peer is loopback (`127.0.0.1`/`::1`) so a LAN client can't spoof the header. Cloudflare path still gets `Secure`; direct LAN HTTP gets the cookie without `Secure` so browsers retain it and the login flow completes. Implemented in `src/kb_dashboard/kb_dashboard/server.py` (~line 115 peer check, ~line 139 cookie emission). Needs on-kart verification next session.
- [2026-04-20] **Dashboard: joystick-style XY plot replaces steering/accel numeric block (default skin)** — Replaced the Actuations + Targets cards in the default skin with one "Control — Target vs Actual" card: 220×220 canvas pad (X=steering, Y=throttle−brake), orange crosshair = target (`orin_cmd_*`), blue dot = actual (`esp32_*`), dashed error line between them; compact TGT/ACT/Δ numeric column on the right with labels left-aligned on the far right (Steer / Throttle / Brake / Str PWM). X-axis follows the kart's `+rad = left turn` convention (REP 103, matches HUD). Other skins untouched. Implemented in `index.html` via new `updateControlPanel()` + `drawControlPad()` helpers. Verified in browser with mock data; deployed to Orin; user confirmed after hard-refresh.
- [2026-04-20] **Shutdown button hidden in non-autonomous missions** — `updateMissionUI()` at `index.html:287` now hides a row's children *individually* (instead of the whole row), skipping any element whose classList includes `shutdown`. All 5 skin Shutdown buttons already had that class except KITT's; added there too. Start/Stop/EBS/Restart still gated behind autonomous missions as before.
- [x] **Wi-Fi fallback failed a real-world event on 2026-08-08: after a full battery power cycle, the Orin came up with no internet.** Rubén replugged the kart battery; the tunnel never registered and he had to plug the phone and configure Wi-Fi by hand.
  **Root cause confirmed 2026-08-10, and it was the cold-boot half.** `journalctl -t wifi-watchdog` shows every boot logging `radio is 'unavailable' with no connection -- falling back to the kart-ap AP` and nothing further, while the unplug path succeeded twice in the same log (2026-07-31 13:10 and 2026-08-08 15:52, both joining `Robots_urjc` within 7 s). The script set `client_attempt_spent=1` before its loop, deliberately, so a boot with no tether kept the AP and never looked for a network. Fixed by starting it at 0. Accepted trade, now in `.agents/orin-environment.md`: an Orin booted in the lab with `Robots_urjc` in range and no phone attached joins it, so the dashboard leaves `10.42.0.1`.
  **Cold-boot path observed working 2026-08-10**, on a real power cycle with no phone plugged in: watchdog started 10:18:58, brought the AP up at 10:19:00, found no tether and nobody associated at 10:19:09, released the AP and joined a known network by 10:19:17 — 19 s from start to internet. Under the old code this boot would have stopped at the AP and stayed unreachable.

- [x 2026-08-10] **Dashboard messages need plain language.** "STEER TRIPPED" told Rubén nothing (2026-08-08). Done in two passes: the steering chip in `9539561` (STEER SIGNAL LOST, naming the emergency brake and the throttle block), and the rest in `df21245` — the heartbeat chip reads `ESP32 SILENT 12.4s` instead of `HB LOST`, the SYS chip names the fault (`SYS NO STEER SIGNAL`, `SYS ESP32 TASK MEMORY LOW`, `SYS CONE DETECTION SLOW`) instead of an acronym, and the seven detail readouts plus the System page cards spell out what each one measures and what a bad value costs. Verified in a browser in demo mode at 1280x720: topbar chips, the status popover and the System page all render without overflow. **Not yet seen on the Orin** — it is in the pending-deploy entry below.

- [x 2026-08-10] **SAFETY: the steering motor is driven whenever an autonomous mission is selected, with no Start pressed** — Fixed in `state_machine_node.py` using suggested options 1+3 from the board entry, without touching firmware or `cmd_vel_bridge`. The state machine now (a) no longer forces PID mode on entering `AS_READY` — that moved to the `start` command, since arming the position loop is a driving action; (b) asserts direct-PWM mode (`ORIN_STEER_MODE` = 1) from its 10 Hz heartbeat whenever an autonomous mission is selected and the state is not `AS_DRIVING`, so the mux's zero Twist means "no drive" instead of "drive to centre and hold" — re-asserting at 10 Hz means a dashboard mode toggle cannot silently re-power the motor before Start. `cone_follower` still re-asserts PWM when the algorithm is None, so the PID force at Start does not fight it. The same-class sweep found one more instance: `throttle_test` published 50% throttle the moment the mission was selected, before Start — its fixed-throttle branch moved inside the `AS_DRIVING` gate. `_publish_steer_mode` now logs only on change (it runs at 10 Hz). On-kart verification is a live board item. Original write-up: `.agents/error-log.md`, 2026-08-10.
